# ContainerFlow — dev API ports
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
services:
  booking-api:
    ports:
      - "8081:8080"

  container-api:
    ports:
      - "8082:8080"

  notification-api:
    ports:
      - "8083:8080"